<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- base border (reuse existing) -->
    <item android:drawable="@drawable/bg_input_border" />

    <!-- right padding area so text doesn't overlap the eye icon -->
    <item>
        <shape>
            <solid android:color="@android:color/transparent" />
            <padding
                android:left="0dp"
                android:top="0dp"
                android:right="46dp"
                android:bottom="0dp" />
        </shape>
    </item>

</layer-list>

